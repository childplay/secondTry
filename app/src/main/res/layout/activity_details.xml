<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/pig"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:id="@+id/activity_details"
    tools:context="com.example.svetoslav.childplay.activity_details">

    <ImageButton
        android:contentDescription="@string/play_sound"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:id="@+id/kiss"
        android:background="@drawable/sound64"/>
    
    <ImageButton
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:id="@+id/spell"
        android:background="@drawable/spell"/>

</LinearLayout>
